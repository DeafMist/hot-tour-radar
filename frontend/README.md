# Frontend

React app for searching tours and cheap tickets.

## Run

```bash
docker compose up -d frontend
```
* React application starts at http://localhost:3000

## Stop

```bash
docker compose down
```